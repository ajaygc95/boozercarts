import styled from "styled-components";
import { Button, Container } from "../../../";

export const RegisterUserContainer = styled(Container)`
  height: 80vh;
  border: 2px solid blue;
`;
export const ContainerWrapper = styled.form`
  height: auto;
  text-align: center;
`;
