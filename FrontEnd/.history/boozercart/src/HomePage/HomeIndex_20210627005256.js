import React, { useState } from "react";

export default function HomeIndex() {
const [values, setValues] = useState(
    [
        {title: 'first one', id:1},
        {title: 'second one', id:1},
        {title: 'first one', id:1},
    ]
)

  return <></>;
}
